import { insertUser } from "../post-users";

describe("Testing users handler", () => {
	const mockEvent = {
		body: JSON.stringify({
			email: "testing@email.com",
			amount: 99
		})
	};
	it("It should");
});